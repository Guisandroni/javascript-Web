console.log(window);
// window é o objeto global do browser
// possui diferentes métodos e propriedades

console.log(window.innerHeight); // retorna a altura do browser

//window.alert("Isso é um alerta");
//alert("Isso é um alerta"); //Funciona

let valor = document.querySelector("div"); // Seleciona o primeiro h1

document.body; // Retorna o body

// Retorne o url da página atual utilizando o objeto windowdo

console.log(document.querySelector("title"));
document.querySelector("title");
let selecionado = document.querySelector("h1");
console.log(selecionado);

console.log(document.querySelector("h1"));
// Seleciona o primeiro elemento da página que
// possua a classe ativo

// Retorne a linguagem do navegador

// Retorne a largura da janela

window.alert("ola");
